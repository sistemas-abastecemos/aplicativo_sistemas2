// Auth

Se implementa memoria de url a la que se queria acceder sin haber estado autenticado, por lo que una vez el usuario se auntentique sera redirigido a la ruta que inicialmente queria acceder

// Inventarios -> Reportes -> Existencias y costos

Se implemento nueva opcion para generar el reporte con un arreglo de lapsos y traer el acumulado consolidado
